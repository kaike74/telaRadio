name = "dashboard-radio-worker"
main = "worker/src/index.js"
compatibility_date = "2024-11-01"
workers_dev = true
account_id = "458e6d616cddf6660731e71235fe4c45"

# KV Namespace
kv_namespaces = [
  { binding = "DASHBOARD_KV", id = "598948c19c524ab3af65831cd8f6278f" }
]

# Routes - Configure com seu domínio do Cloudflare Pages
# routes = [
#   { pattern = "dashboard.seudominio.com/api/*", zone_name = "seudominio.com" }
# ]

[build]
command = ""

[build.upload]
format = "service-worker"

# Variáveis de ambiente (use secrets para valores sensíveis)
[env.production]
name = "dashboard-radio-worker"

# Para configurar secrets (API keys), use:
# wrangler secret put API_KEY
# wrangler secret put GEONAMES_USERNAME
